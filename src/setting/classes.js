import React, { Component } from "react";
import { ReactDOM } from "react";

class ScheduleClass extends Component{
    render(){
        return(
            <label>Clinic Schedule for</label>
        )
    }
}